# 个人费用支出管理系统 - 系统设计文档

## 一、产品概述

### 1.1 产品定位

面向经常出差的职场人士，提供发票管理、费用报销、出差补贴管理的一站式解决方案。通过 H5 技术开发，封装为微信小程序，免安装即用，同时支持 PC 端浏览器访问。

### 1.2 核心痛点

| 痛点 | 解决方案 |
|------|---------|
| 发票多、整理麻烦 | PDF 发票拖入即自动解析，生成待报销记录 |
| 忘记报销导致损失 | 待报销提醒 + 统计面板，一目了然 |
| 出差补贴容易遗漏 | 出差日期自动计算补贴天数 |
| 报销单手工制作繁琐 | 一键生成标准报销单 PDF |

### 1.3 目标用户

- 经常出差的企业员工
- 自由职业者 / 个体经营者
- 需要管理个人支出的职场人士

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      客户端层                            │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ 微信小程序 │  │  H5 移动端   │  │   PC 浏览器端    │   │
│  │ (壳封装)  │  │  (Vue3+Vant) │  │ (Vue3+ElementPlus)│  │
│  └────┬─────┘  └──────┬───────┘  └────────┬─────────┘   │
│       │               │                   │              │
│       └───────────────┼───────────────────┘              │
│                       │ HTTPS                            │
└───────────────────────┼──────────────────────────────────┘
                        │
┌───────────────────────┼──────────────────────────────────┐
│                  网关 & 负载均衡                          │
│              Nginx / Spring Cloud Gateway                │
└───────────────────────┼──────────────────────────────────┘
                        │
┌───────────────────────┼──────────────────────────────────┐
│                   后端服务层                              │
│  ┌─────────────────────────────────────────────────┐     │
│  │            Spring Boot 3.x 应用                  │     │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │     │
│  │  │用户模块   │ │费用模块   │ │报销模块          │ │     │
│  │  ├──────────┤ ├──────────┤ ├──────────────────┤ │     │
│  │  │会员模块   │ │发票解析   │ │统计分析模块      │ │     │
│  │  ├──────────┤ ├──────────┤ ├──────────────────┤ │     │
│  │  │支付模块   │ │分享模块   │ │PDF生成模块       │ │     │
│  │  └──────────┘ └──────────┘ └──────────────────┘ │     │
│  └─────────────────────────────────────────────────┘     │
└───────────────────────┼──────────────────────────────────┘
                        │
┌───────────────────────┼──────────────────────────────────┐
│                   数据 & 存储层                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐   │
│  │  MySQL   │  │  Redis   │  │  MinIO   │  │ 消息队列│   │
│  │ 8.0+    │  │  缓存     │  │ 文件存储  │  │RabbitMQ│   │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘   │
└──────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端 - 移动端** | Vue 3 + Vant 4 + TypeScript | 移动端 H5 组件库，适配微信小程序 |
| **前端 - PC 端** | Vue 3 + Element Plus + TypeScript | PC 端组件库 |
| **前端构建** | Vite 5 | 快速构建工具 |
| **状态管理** | Pinia | Vue 3 推荐状态管理 |
| **后端框架** | Spring Boot 3.2+ | Java 17+ |
| **ORM** | MyBatis-Plus | 简化数据库操作 |
| **数据库** | MySQL 8.0 | 主数据库 |
| **缓存** | Redis 7 | 会话缓存、验证码、限流 |
| **文件存储** | MinIO / 阿里云 OSS | 发票 PDF、报销单存储 |
| **PDF 解析** | Apache PDFBox | 解析发票 PDF |
| **PDF 生成** | iText 7 / Flying Saucer | 生成报销单 PDF |
| **消息队列** | RabbitMQ | 异步任务处理（PDF 解析等） |
| **支付** | 微信支付 SDK + 支付宝 SDK | 会员缴费 |
| **小程序** | 微信小程序 web-view | 封装 H5 页面 |
| **部署** | Docker + Docker Compose | 容器化部署 |

### 2.3 前端架构

```
前端项目 (Monorepo - pnpm workspace)
├── packages/
│   ├── shared/              # 共享代码
│   │   ├── api/             # API 请求封装
│   │   ├── types/           # TypeScript 类型定义
│   │   ├── utils/           # 工具函数
│   │   └── stores/          # Pinia 状态管理
│   ├── mobile/              # H5 移动端 (Vant 4)
│   │   ├── src/
│   │   │   ├── views/       # 页面
│   │   │   ├── components/  # 组件
│   │   │   ├── router/      # 路由
│   │   │   └── styles/      # 样式 (Apple 风格)
│   │   └── index.html
│   ├── desktop/             # PC 端 (Element Plus)
│   │   ├── src/
│   │   │   ├── views/
│   │   │   ├── components/
│   │   │   ├── router/
│   │   │   └── styles/
│   │   └── index.html
│   └── miniprogram/         # 微信小程序壳
│       ├── pages/
│       │   └── webview/     # web-view 页面
│       └── app.json
└── pnpm-workspace.yaml
```

---

## 三、功能模块设计

### 3.1 功能模块总览

```
┌─────────────────────────────────────────────────┐
│                  费用管理系统                     │
├─────────┬─────────┬─────────┬─────────┬─────────┤
│ 用户模块 │ 费用模块 │ 报销模块 │ 统计模块 │ 运营模块 │
├─────────┼─────────┼─────────┼─────────┼─────────┤
│ · 注册  │ · 发票  │ · 报销单 │ · 概览  │ · 会员  │
│ · 登录  │   管理  │   生成  │ · 出差  │   管理  │
│ · 个人  │ · 补贴  │ · 报销单 │   统计  │ · 支付  │
│   中心  │   管理  │   PDF   │ · 费用  │ · 分享  │
│ · 设置  │ · 费用  │ · 收款  │   分析  │   推广  │
│         │   分类  │   确认  │ · 趋势  │ · 返佣  │
│         │         │         │   图表  │         │
└─────────┴─────────┴─────────┴─────────┴─────────┘
```

### 3.2 用户模块

#### 3.2.1 注册 / 登录

- **微信授权登录**：小程序环境直接获取微信授权（主流程）
- **手机号登录**：H5 / PC 端通过手机号 + 短信验证码登录
- **邀请码绑定**：注册时可填写邀请码，绑定推荐关系

#### 3.2.2 个人中心

- 个人资料编辑（头像、昵称、公司名称、部门）
- 会员状态展示（试用期/已付费/已过期）
- 我的邀请（邀请记录、返佣统计）
- 设置（通知偏好、默认补贴标准）

### 3.3 费用管理模块

#### 3.3.1 发票管理

**核心交互流程：**

```
用户拖入/选择 PDF 发票
        │
        ▼
  上传至服务器 (MinIO)
        │
        ▼
  异步解析发票 (PDFBox)
  ┌─────┴─────┐
  │ 解析字段:  │
  │ · 发票号码 │
  │ · 发票代码 │
  │ · 开票日期 │
  │ · 金额(含税)│
  │ · 税额     │
  │ · 销方名称 │
  │ · 购方名称 │
  │ · 发票类型 │
  └─────┬─────┘
        ▼
  生成「待报销」支出记录
        │
        ▼
  用户确认/修改信息
```

**发票解析策略：**
- 电子发票 PDF：使用 PDFBox 提取文本，正则匹配关键字段
- 增值税发票：识别发票代码、号码、金额、税额等标准字段
- 火车票/机票：识别出发地、目的地、金额、日期
- 解析失败时：提示用户手动填写关键信息（金额、发票号必填）

**发票分类：**

| 类别 | 说明 |
|------|------|
| 交通费 | 机票、火车票、出租车、网约车 |
| 住宿费 | 酒店发票 |
| 餐饮费 | 餐厅发票 |
| 办公费 | 办公用品发票 |
| 通讯费 | 电话费发票 |
| 其他 | 用户自定义类别 |

#### 3.3.2 出差补贴管理

```
创建出差记录
├── 出差目的地
├── 出差开始日期
├── 出差结束日期
├── 补贴标准 (元/天，可在设置中配置默认值)
└── 自动计算
    ├── 出差天数
    └── 补贴总金额 = 天数 × 标准
```

#### 3.3.3 手动添加费用

- 支持手动创建费用记录（无发票场景）
- 必填：金额、日期、类别
- 选填：描述、关联出差记录

### 3.4 报销模块

#### 3.4.1 报销单生成

**操作流程：**

```
  选择待报销的费用项
  (多选：发票费用 + 补贴)
        │
        ▼
  输入报销信息
  ├── 报销单标题 (如: 2026年3月北京出差)
  ├── 备注说明
  └── 关联出差记录 (可选)
        │
        ▼
  预览报销单
        │
        ▼
  确认生成 → 生成 PDF 报销单
        │
        ▼
  状态变为「已提交」
```

#### 3.4.2 报销单 PDF 内容

```
┌──────────────────────────────────────────────────┐
│                   费用报销单                      │
│                                                  │
│  报销人：张三           日期：2026-03-15          │
│  部门：技术部           报销单号：RB202603150001  │
│                                                  │
│  ┌────┬──────────┬────────┬────────┬──────────┐  │
│  │序号│ 费用类别  │ 发票号码 │  金额  │   备注   │  │
│  ├────┼──────────┼────────┼────────┼──────────┤  │
│  │ 1  │ 交通费   │044001..│ 580.00 │北京→上海 │  │
│  │ 2  │ 住宿费   │031022..│ 450.00 │如家酒店  │  │
│  │ 3  │ 餐饮费   │无       │ 120.00 │工作餐   │  │
│  │ 4  │ 出差补贴 │  -     │ 300.00 │3天×100  │  │
│  ├────┼──────────┼────────┼────────┼──────────┤  │
│  │合计│          │        │1450.00 │          │  │
│  └────┴──────────┴────────┴────────┴──────────┘  │
│                                                  │
│  备注：2026年3月12日-14日北京出差费用             │
│                                                  │
│  报销人签字：________  审批人签字：________       │
│  日期：________        日期：________             │
└──────────────────────────────────────────────────┘
```

#### 3.4.3 报销状态流转

```
待报销 ──→ 已提交(生成报销单) ──→ 已收款 ──→ 完成
                                    │
                                    └── 用户点击「已收钱」确认
```

### 3.5 统计分析模块

#### 3.5.1 概览仪表盘

```
┌─────────────────────────────────────────────────┐
│                  我的费用概览                     │
│                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  ¥12,580 │  │  ¥8,200  │  │  ¥4,380  │       │
│  │  总支出   │  │  已报销   │  │  待报销   │       │
│  └──────────┘  └──────────┘  └──────────┘       │
│                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │   45天   │  │   12次   │  │   38张   │       │
│  │ 出差总天数│  │  出差次数 │  │  发票总数 │       │
│  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────┘
```

#### 3.5.2 统计报表清单

| 报表名称 | 维度 | 展示形式 |
|----------|------|---------|
| **费用月度趋势** | 按月汇总支出金额 | 折线图 |
| **费用分类占比** | 按分类统计金额占比 | 饼图/环形图 |
| **出差统计** | 出差次数、天数、城市分布 | 柱状图 + 表格 |
| **报销进度** | 待报销 vs 已报销 vs 已收款 | 堆叠柱状图 |
| **月度报销率** | 每月已报销/总支出比率 | 进度条 |
| **年度费用对比** | 同比去年各月支出 | 双折线图 |
| **城市出差排行** | 按城市统计出差频次和费用 | 排行榜 |
| **费用日历** | 按日历视图展示每天支出 | 日历热力图 |

#### 3.5.3 数据导出

- 支持导出 Excel 报表
- 支持按时间范围筛选
- 支持按费用类别筛选

### 3.6 会员 & 支付模块

#### 3.6.1 会员体系

```
┌──────────────────────────────────────────────┐
│               会员体系设计                    │
│                                              │
│  ┌─────────────┐      ┌─────────────┐       │
│  │  免费试用    │      │  年度会员    │       │
│  │             │      │             │       │
│  │ · 30天免费   │ ──→ │ · 99元/年   │       │
│  │ · 全功能    │      │ · 全功能    │       │
│  │ · 到期提醒   │      │ · 自动续费   │       │
│  └─────────────┘      └─────────────┘       │
│                                              │
│  试用到期后：                                 │
│  · 可查看历史数据（只读）                     │
│  · 不可新增费用、生成报销单                   │
│  · 弹窗引导付费                              │
└──────────────────────────────────────────────┘
```

#### 3.6.2 支付流程

```
用户点击「开通会员」
        │
        ▼
  选择支付方式
  ├── 微信支付
  └── 支付宝支付
        │
        ▼
  创建订单 → 调用支付接口
        │
        ▼
  支付回调确认
        │
        ▼
  激活会员 (有效期+365天)
        │
        ▼
  检查是否有推荐人
  └── 有 → 给推荐人返佣19元
```

### 3.7 分享推广模块

#### 3.7.1 邀请机制

```
用户A (推荐人)
    │
    ├── 生成专属邀请链接/二维码
    │   (链接包含邀请码参数)
    │
    ├── 分享到朋友圈 / 发送给好友
    │
    └── 用户B 通过链接注册
        │
        ├── 用户B 注册成功，绑定推荐关系
        │
        └── 用户B 付费成为会员
            │
            └── 用户A 获得 19元 返佣
                (可提现到微信/支付宝)
```

#### 3.7.2 朋友圈分享模板

**模板一：痛点型**
```
┌──────────────────────────────────┐
│  🎯 还在手动整理报销发票？       │
│                                  │
│  拍照/拖入发票PDF               │
│  → 自动识别发票信息              │
│  → 一键生成报销单                │
│                                  │
│  出差补贴再也不会忘记报销了！     │
│                                  │
│  [扫码免费试用30天]              │
│  ┌────────────┐                  │
│  │  小程序码  │                  │
│  └────────────┘                  │
└──────────────────────────────────┘
```

**模板二：数据型**
```
┌──────────────────────────────────┐
│  💰 用了这个工具后：             │
│                                  │
│  · 报销效率提升 80%              │
│  · 再也没忘记过出差补贴          │
│  · 每月多回血 XXXX 元            │
│                                  │
│  发票拖进去就完事了              │
│  [长按识别，免费试用]            │
└──────────────────────────────────┘
```

**模板三：简洁型**
```
┌──────────────────────────────────┐
│  安利一个报销神器 ✨              │
│                                  │
│  发票PDF拖进去                   │
│  自动出报销单                    │
│  出差补贴自动算                  │
│                                  │
│  同事们都在用，免费试用30天      │
│  👇 扫码体验                     │
└──────────────────────────────────┘
```

#### 3.7.3 分享功能

- **生成分享海报**：包含用户专属二维码的精美海报图片
- **一键复制文案**：点击即复制预设的朋友圈文案
- **分享到微信好友**：调用微信分享 API
- **分享到朋友圈**：调用微信朋友圈分享 API
- **生成邀请链接**：可直接发送链接，点击即可打开小程序注册

---

## 四、数据库设计

### 4.1 ER 图概览

```
用户(user) ──1:N── 费用记录(expense) ──N:1── 费用分类(expense_category)
    │                    │
    │                    N:1
    │                    │
    │              出差记录(business_trip)
    │
    ├──1:N── 报销单(reimbursement) ──M:N── 费用记录(expense)
    │
    ├──1:N── 会员订单(member_order)
    │
    ├──1:N── 邀请记录(invitation)
    │
    └──1:N── 返佣记录(commission)
```

### 4.2 核心表结构

#### 4.2.1 用户表 (t_user)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| openid | VARCHAR(64) | 微信 openid |
| unionid | VARCHAR(64) | 微信 unionid |
| phone | VARCHAR(20) | 手机号 |
| nickname | VARCHAR(50) | 昵称 |
| avatar_url | VARCHAR(500) | 头像 |
| company | VARCHAR(100) | 公司名称 |
| department | VARCHAR(50) | 部门 |
| invite_code | VARCHAR(16) | 我的邀请码 (唯一) |
| inviter_id | BIGINT | 邀请人用户ID |
| member_status | TINYINT | 会员状态: 0试用 1有效 2过期 |
| member_expire_time | DATETIME | 会员到期时间 |
| trial_start_time | DATETIME | 试用开始时间 |
| default_subsidy | DECIMAL(10,2) | 默认补贴标准(元/天) |
| status | TINYINT | 账号状态: 0正常 1禁用 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 4.2.2 费用分类表 (t_expense_category)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| name | VARCHAR(30) | 分类名称 |
| icon | VARCHAR(50) | 图标 |
| sort_order | INT | 排序 |
| is_system | TINYINT | 是否系统预设: 0否 1是 |
| user_id | BIGINT | 用户ID (自定义分类) |
| status | TINYINT | 状态: 0正常 1删除 |

#### 4.2.3 出差记录表 (t_business_trip)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| title | VARCHAR(100) | 出差标题 |
| destination | VARCHAR(100) | 目的地 |
| start_date | DATE | 开始日期 |
| end_date | DATE | 结束日期 |
| days | INT | 出差天数 |
| subsidy_per_day | DECIMAL(10,2) | 补贴标准(元/天) |
| subsidy_total | DECIMAL(10,2) | 补贴总额 |
| remark | VARCHAR(500) | 备注 |
| status | TINYINT | 状态: 0正常 1删除 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 4.2.4 费用记录表 (t_expense)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| category_id | BIGINT | 分类ID |
| trip_id | BIGINT | 关联出差记录ID (可空) |
| type | TINYINT | 类型: 1发票费用 2出差补贴 3手动录入 |
| amount | DECIMAL(10,2) | 金额 |
| tax_amount | DECIMAL(10,2) | 税额 |
| invoice_no | VARCHAR(30) | 发票号码 |
| invoice_code | VARCHAR(20) | 发票代码 |
| invoice_date | DATE | 开票日期 |
| invoice_type | VARCHAR(30) | 发票类型 |
| seller_name | VARCHAR(200) | 销方名称 |
| buyer_name | VARCHAR(200) | 购方名称 |
| file_url | VARCHAR(500) | 发票文件URL |
| file_name | VARCHAR(200) | 原始文件名 |
| description | VARCHAR(500) | 费用描述 |
| expense_date | DATE | 费用日期 |
| reimburse_status | TINYINT | 报销状态: 0待报销 1已提交 2已收款 |
| reimbursement_id | BIGINT | 所属报销单ID |
| status | TINYINT | 状态: 0正常 1删除 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 4.2.5 报销单表 (t_reimbursement)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| reimburse_no | VARCHAR(20) | 报销单号 |
| title | VARCHAR(100) | 报销单标题 |
| total_amount | DECIMAL(10,2) | 总金额 |
| item_count | INT | 费用项数量 |
| remark | VARCHAR(500) | 备注 |
| pdf_url | VARCHAR(500) | 报销单PDF的URL |
| trip_id | BIGINT | 关联出差记录 (可空) |
| reimburse_status | TINYINT | 状态: 0已提交 1已收款 |
| received_at | DATETIME | 收款时间 |
| status | TINYINT | 状态: 0正常 1删除 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 4.2.6 会员订单表 (t_member_order)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| order_no | VARCHAR(32) | 订单号 |
| amount | DECIMAL(10,2) | 订单金额 |
| pay_type | TINYINT | 支付方式: 1微信 2支付宝 |
| pay_status | TINYINT | 支付状态: 0未支付 1已支付 2已退款 |
| pay_time | DATETIME | 支付时间 |
| trade_no | VARCHAR(64) | 第三方交易号 |
| member_start | DATETIME | 会员生效时间 |
| member_end | DATETIME | 会员到期时间 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 4.2.7 邀请记录表 (t_invitation)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| inviter_id | BIGINT | 邀请人ID |
| invitee_id | BIGINT | 被邀请人ID |
| invitee_phone | VARCHAR(20) | 被邀请人手机号 |
| has_paid | TINYINT | 是否已付费: 0否 1是 |
| paid_at | DATETIME | 付费时间 |
| created_at | DATETIME | 创建时间 |

#### 4.2.8 返佣记录表 (t_commission)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 获佣用户ID |
| invitation_id | BIGINT | 关联邀请记录 |
| order_id | BIGINT | 关联订单 |
| amount | DECIMAL(10,2) | 返佣金额 |
| withdraw_status | TINYINT | 提现状态: 0待提现 1已提现 |
| withdraw_time | DATETIME | 提现时间 |
| withdraw_type | TINYINT | 提现方式: 1微信 2支付宝 |
| created_at | DATETIME | 创建时间 |

---

## 五、API 接口设计

### 5.1 接口规范

- **Base URL**: `/api/v1`
- **认证方式**: JWT Token (Header: `Authorization: Bearer {token}`)
- **统一响应格式**:

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 5.2 接口清单

#### 用户模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/wx-login` | 微信授权登录 |
| POST | `/auth/sms-login` | 手机号验证码登录 |
| POST | `/auth/send-sms` | 发送短信验证码 |
| GET | `/user/profile` | 获取个人资料 |
| PUT | `/user/profile` | 更新个人资料 |
| GET | `/user/member-status` | 获取会员状态 |

#### 费用管理模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/expense/upload-invoice` | 上传发票PDF并解析 |
| POST | `/expense` | 手动创建费用记录 |
| PUT | `/expense/{id}` | 更新费用记录 |
| DELETE | `/expense/{id}` | 删除费用记录 |
| GET | `/expense/list` | 费用列表 (分页、筛选) |
| GET | `/expense/{id}` | 费用详情 |
| GET | `/expense/pending` | 待报销费用列表 |

#### 出差管理模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/trip` | 创建出差记录 |
| PUT | `/trip/{id}` | 更新出差记录 |
| DELETE | `/trip/{id}` | 删除出差记录 |
| GET | `/trip/list` | 出差记录列表 |
| GET | `/trip/{id}` | 出差详情 (含关联费用) |

#### 报销模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/reimbursement/generate` | 生成报销单 |
| GET | `/reimbursement/list` | 报销单列表 |
| GET | `/reimbursement/{id}` | 报销单详情 |
| GET | `/reimbursement/{id}/pdf` | 下载报销单PDF |
| PUT | `/reimbursement/{id}/received` | 确认已收款 |

#### 统计分析模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/stats/overview` | 概览数据 |
| GET | `/stats/monthly-trend` | 月度费用趋势 |
| GET | `/stats/category-ratio` | 分类占比 |
| GET | `/stats/trip-summary` | 出差统计 |
| GET | `/stats/reimburse-progress` | 报销进度统计 |
| GET | `/stats/yearly-compare` | 年度对比 |
| GET | `/stats/city-ranking` | 城市排行 |
| GET | `/stats/export` | 导出报表(Excel) |

#### 会员 & 支付模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/pay/create-order` | 创建支付订单 |
| POST | `/pay/wx-notify` | 微信支付回调 |
| POST | `/pay/ali-notify` | 支付宝支付回调 |
| GET | `/pay/order/{orderNo}` | 查询订单状态 |

#### 分享推广模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/share/invite-code` | 获取邀请码/链接 |
| GET | `/share/poster` | 生成分享海报图片 |
| GET | `/share/templates` | 获取分享文案模板 |
| GET | `/share/invite-records` | 我的邀请记录 |
| GET | `/share/commission` | 返佣记录 |
| POST | `/share/withdraw` | 申请提现 |

---

## 六、前端页面设计

### 6.1 设计风格

- **设计语言**：Apple Design 风格
- **关键词**：简洁、扁平、圆角、毛玻璃效果、大留白
- **配色方案**：

```
主色调：#007AFF (Apple Blue)
成功色：#34C759 (Apple Green)
警告色：#FF9500 (Apple Orange)
危险色：#FF3B30 (Apple Red)
背景色：#F2F2F7 (浅灰背景)
卡片色：#FFFFFF (白色卡片)
文字主色：#1C1C1E
文字副色：#8E8E93
分割线色：#C6C6C8
```

- **字体**：`-apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Helvetica Neue', sans-serif`
- **圆角**：统一使用 12px-16px 大圆角
- **阴影**：轻柔阴影 `0 2px 12px rgba(0,0,0,0.08)`
- **动效**：页面切换使用 iOS 风格的滑动过渡

### 6.2 移动端页面结构 (H5 / 小程序)

```
底部 Tab 导航 (4个)
├── 首页 (概览仪表盘)
│   ├── 费用概览卡片 (总额/已报/待报)
│   ├── 快捷操作 (上传发票/添加补贴/生成报销单)
│   ├── 最近费用列表
│   └── 待报销提醒
│
├── 费用 (费用管理)
│   ├── 费用列表 (全部/待报销/已报销)
│   │   └── 筛选: 时间/分类/状态
│   ├── 上传发票页 (拖拽区域 + 文件选择)
│   ├── 费用详情页
│   └── 手动添加费用页
│
├── 报销 (报销管理)
│   ├── 报销单列表 (已提交/已收款)
│   ├── 创建报销单页 (选择费用项)
│   ├── 报销单详情页
│   └── 报销单PDF预览
│
└── 我的 (个人中心)
    ├── 个人信息
    ├── 会员中心 (开通/续费)
    ├── 出差管理 (出差记录列表)
    ├── 统计报表
    ├── 分享推广 (邀请/返佣)
    └── 设置 (通知/补贴标准/关于)
```

### 6.3 PC 端页面结构

```
顶部导航栏 + 左侧菜单
├── 仪表盘 (Dashboard)
│   ├── 数据概览卡片
│   ├── 费用趋势图
│   ├── 分类占比图
│   └── 待报销提醒
│
├── 费用管理
│   ├── 费用列表 (表格视图，支持拖拽上传区域)
│   ├── 费用详情弹窗
│   └── 批量操作
│
├── 出差管理
│   ├── 出差记录列表
│   └── 出差详情 (含关联费用)
│
├── 报销中心
│   ├── 报销单列表
│   ├── 创建报销单
│   └── 报销单详情/PDF预览
│
├── 统计分析
│   ├── 月度趋势
│   ├── 分类分析
│   ├── 出差统计
│   ├── 报销进度
│   ├── 年度对比
│   ├── 城市排行
│   └── 费用日历
│
└── 个人中心
    ├── 个人信息
    ├── 会员管理
    ├── 分享推广
    └── 系统设置
```

### 6.4 关键页面交互设计

#### 6.4.1 发票上传页 (移动端)

```
┌─────────────────────────────────┐
│  ←  上传发票                     │
├─────────────────────────────────┤
│                                 │
│  ┌─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐  │
│  │                           │  │
│  │    📄                     │  │
│  │                           │  │
│  │  将发票PDF拖入此处         │  │
│  │  或点击选择文件            │  │
│  │                           │  │
│  │  支持 PDF 格式             │  │
│  └─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘  │
│                                 │
│  ── 最近上传 ──────────────── │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 📄 火车票_北京_上海.pdf  │   │
│  │ ¥580.00  |  解析成功 ✓   │   │
│  │ 发票号: 044001...        │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 📄 酒店发票.pdf          │   │
│  │ 解析中... ◌              │   │
│  └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

#### 6.4.2 创建报销单页

```
┌─────────────────────────────────┐
│  ←  创建报销单                   │
├─────────────────────────────────┤
│                                 │
│  报销单标题                     │
│  ┌─────────────────────────┐   │
│  │ 2026年3月北京出差         │   │
│  └─────────────────────────┘   │
│                                 │
│  选择费用项 (已选3项 ¥1,450)    │
│                                 │
│  ☑ 📄 火车票      ¥580.00     │
│  ☑ 🏨 酒店住宿    ¥450.00     │
│  ☑ 🍽 工作餐      ¥120.00     │
│  ☐ 🚕 出租车      ¥35.00      │
│                                 │
│  ☑ 出差补贴 (3天×¥100)         │
│    ¥300.00                     │
│                                 │
│  备注                           │
│  ┌─────────────────────────┐   │
│  │ 3月12-14日北京出差费用    │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │   合计: ¥1,450.00       │   │
│  │                         │   │
│  │   [ 预览报销单 ]        │   │
│  │   [ 生成报销单PDF ]     │   │
│  └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

#### 6.4.3 会员中心页

```
┌─────────────────────────────────┐
│  ←  会员中心                     │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐   │
│  │  ⭐ 年度会员              │   │
│  │                         │   │
│  │  ¥99/年                 │   │
│  │                         │   │
│  │  ✓ 无限上传发票          │   │
│  │  ✓ 无限生成报销单        │   │
│  │  ✓ 全部统计报表          │   │
│  │  ✓ 数据导出              │   │
│  │  ✓ 30天免费试用          │   │
│  │                         │   │
│  │ ┌───────────┐ ┌────────┐│   │
│  │ │  微信支付  │ │支付宝  ││   │
│  │ └───────────┘ └────────┘│   │
│  │                         │   │
│  │  [  立即开通  ]          │   │
│  └─────────────────────────┘   │
│                                 │
│  ── 推荐好友 ──────────────── │
│                                 │
│  每推荐一位付费好友             │
│  返佣 ¥19                      │
│                                 │
│  [ 生成邀请海报 ]              │
│  [ 复制邀请链接 ]              │
│                                 │
│  已邀请: 5人  已返佣: ¥57      │
│                                 │
└─────────────────────────────────┘
```

---

## 七、后端项目结构

```
expense-manager/
├── pom.xml
├── src/main/java/com/expense/
│   ├── ExpenseApplication.java
│   │
│   ├── common/                          # 通用模块
│   │   ├── config/                      # 配置类
│   │   │   ├── WebMvcConfig.java
│   │   │   ├── RedisConfig.java
│   │   │   ├── MinioConfig.java
│   │   │   ├── RabbitMQConfig.java
│   │   │   └── SwaggerConfig.java
│   │   ├── exception/                   # 异常处理
│   │   │   ├── BizException.java
│   │   │   └── GlobalExceptionHandler.java
│   │   ├── response/                    # 统一响应
│   │   │   ├── Result.java
│   │   │   └── ResultCode.java
│   │   ├── interceptor/                 # 拦截器
│   │   │   ├── AuthInterceptor.java
│   │   │   └── MemberInterceptor.java
│   │   └── util/                        # 工具类
│   │       ├── JwtUtil.java
│   │       ├── InvoiceParser.java
│   │       ├── PdfGenerator.java
│   │       └── SnowflakeIdUtil.java
│   │
│   ├── module/
│   │   ├── auth/                        # 认证模块
│   │   │   ├── controller/
│   │   │   ├── service/
│   │   │   └── dto/
│   │   │
│   │   ├── user/                        # 用户模块
│   │   │   ├── controller/
│   │   │   ├── service/
│   │   │   ├── mapper/
│   │   │   ├── entity/
│   │   │   └── dto/
│   │   │
│   │   ├── expense/                     # 费用模块
│   │   │   ├── controller/
│   │   │   │   ├── ExpenseController.java
│   │   │   │   └── TripController.java
│   │   │   ├── service/
│   │   │   │   ├── ExpenseService.java
│   │   │   │   ├── TripService.java
│   │   │   │   └── InvoiceParseService.java
│   │   │   ├── mapper/
│   │   │   ├── entity/
│   │   │   ├── dto/
│   │   │   └── listener/               # MQ 消费者
│   │   │       └── InvoiceParseListener.java
│   │   │
│   │   ├── reimbursement/              # 报销模块
│   │   │   ├── controller/
│   │   │   ├── service/
│   │   │   │   ├── ReimbursementService.java
│   │   │   │   └── PdfExportService.java
│   │   │   ├── mapper/
│   │   │   ├── entity/
│   │   │   └── dto/
│   │   │
│   │   ├── stats/                      # 统计模块
│   │   │   ├── controller/
│   │   │   ├── service/
│   │   │   └── vo/
│   │   │
│   │   ├── payment/                    # 支付模块
│   │   │   ├── controller/
│   │   │   ├── service/
│   │   │   │   ├── WxPayService.java
│   │   │   │   └── AliPayService.java
│   │   │   ├── mapper/
│   │   │   ├── entity/
│   │   │   └── dto/
│   │   │
│   │   └── share/                      # 分享模块
│   │       ├── controller/
│   │       ├── service/
│   │       │   ├── ShareService.java
│   │       │   ├── CommissionService.java
│   │       │   └── PosterService.java
│   │       ├── mapper/
│   │       ├── entity/
│   │       └── dto/
│   │
│   └── task/                           # 定时任务
│       ├── MemberExpireTask.java        # 会员到期检查
│       └── ReimburseRemindTask.java     # 报销提醒
│
├── src/main/resources/
│   ├── application.yml
│   ├── application-dev.yml
│   ├── application-prod.yml
│   ├── mapper/                          # MyBatis XML
│   └── templates/
│       └── reimbursement.html           # 报销单 PDF 模板
│
└── src/test/
```

---

## 八、部署架构

### 8.1 Docker Compose 部署

```
docker-compose.yml
├── expense-backend     (Java 后端, 端口 8080)
├── expense-mobile      (H5 移动端, 端口 3000)
├── expense-desktop     (PC 端, 端口 3001)
├── nginx               (反向代理, 端口 80/443)
├── mysql               (数据库, 端口 3306)
├── redis               (缓存, 端口 6379)
├── minio               (文件存储, 端口 9000)
└── rabbitmq            (消息队列, 端口 5672/15672)
```

### 8.2 Nginx 路由规则

```
/api/*          → expense-backend:8080
/m/*            → expense-mobile:3000     (移动端H5)
/               → expense-desktop:3001    (PC端)
/files/*        → minio:9000              (文件访问)
```

---

## 九、安全设计

| 安全项 | 措施 |
|--------|------|
| 接口认证 | JWT Token + Refresh Token 双 Token 机制 |
| 数据加密 | 敏感数据 AES 加密存储，传输全程 HTTPS |
| 文件安全 | 文件上传类型校验、大小限制(单文件10MB)、病毒扫描 |
| 接口限流 | Redis 令牌桶限流，防止恶意请求 |
| SQL 注入 | MyBatis 参数化查询，禁止拼接 SQL |
| XSS 防护 | 输入输出转义，CSP 策略 |
| 支付安全 | 签名验证、幂等处理、金额二次校验 |
| 数据隔离 | 用户数据严格按 user_id 隔离 |

---

## 十、关键业务流程

### 10.1 发票上传 → 报销完整流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  上传PDF  │───→│ 异步解析  │───→│ 生成费用  │───→│ 用户确认  │
│  发票文件 │    │ 发票内容  │    │  记录    │    │ /修改信息 │
└──────────┘    └──────────┘    └──────────┘    └────┬─────┘
                                                     │
┌──────────┐    ┌──────────┐    ┌──────────┐         │
│  点击    │←───│ 下载PDF   │←───│ 生成报销  │←────────┘
│ "已收钱"  │    │ 报销单   │    │  单PDF   │  选择待报销项
└──────────┘    └──────────┘    └──────────┘  输入备注 → 生成
     │
     ▼
 状态→已收款
   完成!
```

### 10.2 会员 + 推荐流程

```
用户A注册 → 30天免费试用
     │
     ├── 试用期内付费 ¥99 → 激活1年会员
     │
     ├── 分享邀请链接到朋友圈
     │       │
     │       ▼
     │   用户B 点击链接 → 打开小程序 → 注册 (绑定A的邀请码)
     │       │
     │       ▼
     │   用户B 付费 ¥99
     │       │
     │       ▼
     └── 用户A 获得 ¥19 返佣 → 可提现
```

---

## 十一、开发计划 (里程碑)

### 第一阶段：核心功能

- 用户注册/登录（微信授权 + 手机号）
- 发票上传与 PDF 解析
- 费用记录管理（CRUD）
- 出差记录与补贴管理
- 报销单生成与 PDF 导出
- 收款确认

### 第二阶段：统计 & 体验优化

- 统计仪表盘（概览、趋势、分类）
- 出差统计报表
- 报销进度追踪
- 数据导出（Excel）
- PC 端适配
- 性能优化

### 第三阶段：商业化

- 会员系统与支付集成（微信/支付宝）
- 试用期管理
- 分享推广系统
- 邀请返佣体系
- 朋友圈海报生成
- 提现功能

### 第四阶段：增强功能

- 费用日历热力图
- 年度费用对比
- 城市出差排行
- 消息通知（待报销提醒、会员到期提醒）
- 微信小程序上线
- 持续迭代优化

---

## 十二、附录

### 12.1 发票 PDF 解析字段映射

| 发票字段 | 正则/关键词 | 对应数据库字段 |
|----------|------------|--------------|
| 发票号码 | `发票号码[:：]\s*(\d{8,20})` | invoice_no |
| 发票代码 | `发票代码[:：]\s*(\d{10,12})` | invoice_code |
| 开票日期 | `开票日期[:：]\s*(\d{4}年\d{1,2}月\d{1,2}日)` | invoice_date |
| 价税合计 | `价税合计.*?[¥￥]\s*([\d,.]+)` | amount |
| 税额 | `税额.*?[¥￥]\s*([\d,.]+)` | tax_amount |
| 销方名称 | `名\s*称[:：]\s*(.+)` (销售方区域) | seller_name |
| 购方名称 | `名\s*称[:：]\s*(.+)` (购买方区域) | buyer_name |

### 12.2 环境变量配置

```yaml
# 数据库
MYSQL_HOST: localhost
MYSQL_PORT: 3306
MYSQL_DATABASE: expense_db
MYSQL_USERNAME: root
MYSQL_PASSWORD: ****

# Redis
REDIS_HOST: localhost
REDIS_PORT: 6379

# MinIO
MINIO_ENDPOINT: http://localhost:9000
MINIO_ACCESS_KEY: ****
MINIO_SECRET_KEY: ****
MINIO_BUCKET: expense-files

# 微信小程序
WX_APP_ID: ****
WX_APP_SECRET: ****

# 微信支付
WX_PAY_MCH_ID: ****
WX_PAY_API_KEY: ****

# 支付宝
ALI_PAY_APP_ID: ****
ALI_PAY_PRIVATE_KEY: ****
ALI_PAY_PUBLIC_KEY: ****

# JWT
JWT_SECRET: ****
JWT_EXPIRE: 7200

# 短信 (阿里云)
SMS_ACCESS_KEY: ****
SMS_SECRET_KEY: ****
SMS_SIGN_NAME: 费用管家
SMS_TEMPLATE_CODE: SMS_****
```
